---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart Hermes
  ansible.builtin.systemd_service:
    name: hermes.service
    state: restarted
    daemon_reload: true
