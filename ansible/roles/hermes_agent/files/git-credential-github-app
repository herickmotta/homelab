#!/bin/sh
# Git credential helper that mints a GitHub App installation token.
set -eu

bindir=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
case "${1:-}" in
  get)
    token=$("$bindir/github-app-token") || exit 1
    printf 'username=x-access-token\npassword=%s\n' "$token"
    ;;
esac
